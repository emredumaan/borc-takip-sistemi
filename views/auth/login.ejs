<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giriş yap - KYK 502 Borç Yönetim Sistemi</title>

    <meta property="og:title" content="Kyk 502 numaralı oda - Borç Yönetim Sistemi">
    <meta property="og:description"
        content="Bu sistem Emre DUMAN tarafından oda arkadaşlarımıza olan borçlarımızın takibini kolaylaştırmak için geliştirilmiştir.">
    <meta property="og:site_name" content="KYK 502 BYS">
    <meta property="og:type" content="website">
    <meta property="og:image" content="/icon512_rounded.png">

    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #0F0F0F;
            color: #fff;
            font-family: 'Poppins', sans-serif;
            padding: 3rem 1rem;
        }

        form {
            display: flex;
            flex-direction: column;
            width: 100%;

        }

        form div {
            display: flex;
            flex-direction: column;
        }
        form div label {
            font-size: .9rem;
            margin: .5rem 0;
        }
        form div input[type=text],form div input[type=password] {
            background-color: #181818;
            border: 1px solid #272727;
            border-radius: 5px;
            padding: .5rem;
            outline: 0;
            margin-bottom: 1rem;
            color: #dddddd;
        }
        form div input[type=text]:hover,form div input[type=password]:hover {
            background-color: #1e1e1e;
            border: 1px solid #343434;
        }
        form div input[type=submit] {
            padding: .7rem;
            background-color: #2555cf0e;
            border: 1px solid #2555cf;
            color: #2555cf;
            border-radius: 5px;
        }
        form div input {
            width: 100%;
        }

        img {
            width: 10rem;
            margin: 4rem auto;
            display: block;
        }
    </style>
</head>

<body>
    <img src="/images/full-logo.svg" alt="KYK 502 Borç Yönetim Sistemi">
    <% if(typeof error !='undefined' ) { %>
        <div class="error">
            <span style="color:red">
                <%= error %>
            </span>
        </div>
        <% } %>
            <form action="/login" method="post">
                <div>
                    <label for="username">Kullanıcı adı</label>
                    <input type="text" name="username">
                </div>
                <div>
                    <label for="password">Parola</label>
                    <input type="password" name="password">
                </div>
                <div>
                    <input type="submit" value="Giriş yap">
                </div>
            </form>
</body>

</html>